import React from 'react';
import ReactDOM from 'react-dom/client';
import MovieAPI from './components/Movics/MovieAPI';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MovieAPI />
  </React.StrictMode>
);


